name: sapo_clone_app
description: "Sapo Clone - Customer Shopping App"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8
  dio: ^5.3.2
  provider: ^6.0.5
  json_annotation: ^4.8.1
  mobile_scanner: ^7.2.0
  intl: ^0.20.2
  stomp_dart_client: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  json_serializable: ^6.8.0
  build_runner: ^2.4.6

flutter:
  uses-material-design: true
